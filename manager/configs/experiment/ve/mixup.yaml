# @package _global_
defaults:
  - ve/ft

configs:
  output_dir: /data/private/mxy/exp/UNITER/finetune/ve/mixup
  max_txt_len: 60
  conf_th: 0.2
  max_bb: 100
  min_bb: 10
  num_bb: 36
  train_batch_size: 8192
  val_batch_size: 8192
  gradient_accumulation_steps: 4
  learning_rate: 8.0e-5
  num_train_steps: 4000
  warmup_steps: 400
  mixup: true

mlflow:
  exp_name: mixup-ve
  task: mixup-ve
  