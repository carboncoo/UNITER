# @package _global_
defaults:
  - ve/ft

configs:
  learning_rate: 5.0e-2
  dropout: 0.0
  num_train_steps: 16000
  prompt_len: 20
  prompt_type: 'soft-prefix-first'
  # no / yes / maybe
  label_mapping: [1185, 4208, 2654]

mlflow:
  exp_name: soft-prompt-ve
  task: prompt-ve
  